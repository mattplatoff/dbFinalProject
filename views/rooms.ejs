<!doctype html>
<html xmlns="http://www.w3.org/1999/xhtml" lang="en" dir="ltr">

<head>
    <meta http-equiv="content-type" content="application/xhtml+xml; charset=UTF-8" />

    <meta http-equiv="X-UA-Compatible" content="chrome=1">
    <meta name="google" value="notranslate">

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap-theme.min.css">

    <title><%=title%></title>
</head>
<body style="padding-top: 70px">
  <nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/"><%=title%></a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <li class="active"><a href="/myaccount">My Account<span class="sr-only">(current)</span></a></li>
          <li><a href="/hotels">Hotels</a></li>
        </ul>
        <form id="signin" class="navbar-form navbar-right">
          <div class="form-group">
            <input name="email" id="loginemail" type="text" placeholder="Email" class="form-control">
          </div>
          <div class="form-group">
            <input name="password" id="loginpassword" type="password" placeholder="Password" class="form-control">
          </div>
          <button type="submit" class="btn btn-success">Sign in</button>
        <a role="button" class="btn btn-info" data-target="#registrationModal" data-toggle="modal">Register</a>
        </form>
      </div>
    </div>
  </nav>
<% roomList = JSON.parse(roomListing);%>
<div class="container">
  <div class="row">
    <button type="button" id="bCart" style="float:right" data-toggle="modal" data-target="#reserveModal" class="btn btn-primary">Cart</button>
    <br ><br>
  </div>
    <div class="row">
        <% roomList.forEach(function(room, index) { %>
          <div class="col-md-4">
              <ul class="list-group">
                  <li class="list-group-item active">Room <%= room['Room_no'] %> | <%=room['Type'] %> - Floor <%=room['Floor_no'] %>
                  <button class="btn btn-default bReserve" data-room='<%= room['Room_no'] %>' data-hotel='<%= room['HotelID'] %>' style="float:right;">Add room to cart</button>
                  <br />
                  <b>$<%=room['Price'] %></b>
                  </li>
                  <li class="list-group-item ">
                  <i>For up to <%=room['Capacity'] %> residents</i>
                  <br />
                  <h4>-<%=room['Description'] %></h4>
                  </li>
                  <li class="list-group-item"> Reviews:
                      <% if(room['Reviews'].length == 0){ %>
                      No reviews yet!
                      <% } %>
                      <ul>
                        <% room['Reviews'].forEach(function(review, index) { %>
                          <li style="list-style:none"><%= review['Rating'] %>/10 <%= review['TextComment'] %></li>
                        <% }); %>
                      </ul>
                  </li>

              </ul>
          </div>
        <% }); %>
    </div>
    <div id="reserveModal" tabindex="-1" class="modal fade" role="dialog">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">×</button>
          <h3>Make a reservation</h3>
        </div>
        <form action="/reserve" method="post" id="reservationForm">
          <div class="modal-body">
            <h4>Please select a reservation period, breakfast, and service quantity.</h4>
            <div id="cartRoom" class="form-group">
            </div>
            <div class="form-group">
            3<!-- foreach breakfast, create a textbox for quantity of each breakfast  -->
            </div>
            <div class="form-group">
      	    4<!-- foreach service, create a texbox for quantity of each service  -->
            </div>
       	    <div class="form-group">
      	    5<!-- dropdown with credit cards to choose from  -->
            </div>
      	    <div id='reservationPrice'>
            6<!-- constantly updating price based on reservation period, breakfast, and service  -->
            </div>
      	     <!-- hidden field with CID -->
          </div>
          <div class="modal-footer">
            <button class="btn" data-dismiss="modal" onclick="document.querySelector('#reservationForm').reset()" aria-hidden="true">Close</button>
            <button type="sumbit" class="btn btn-primary">Submit</button>
          </div>
        </form>
      </div>
  </div>
</div>
<script src="//code.jquery.com/jquery-1.11.3.min.js"></script>
<script>
$(function(){
  updateCart();
});
function setCartCookie(value) {
    document.cookie = "cartcookie=|" + value + ";";
}

function removeCartCookie(value){
  var location = getCartCookie().indexOf(value);
  if(location != -1){
    document.cookie = "cartcookie=" + getCartCookie().replace("|" + value,"");
  }
  updateCart();
}

function appendCartCookie(value) {
   if(getCartCookie().indexOf(value) == -1){
     document.cookie = "cartcookie=" + getCartCookie() + "|" + value + ';';
   }
   updateCart();
}

function getCartCookie() {
    var name = "cartcookie=";
    var ca = document.cookie.split(';');
    for(var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "";
}

function updateCart(){
  var rooms = getCartCookie().split('|');
  $('#cartRoom').empty();
  rooms.forEach(function(room, index){
    if(index == 0){
    return;
    }
    var r = room.split(',');
    $('#cartRoom').append('<div class="form-group"><b>Room ' + r[1] + ' in Hotel ' + r[0] +
    '<button class="btn btn-link delRoom" style="float:right;font-size:20px" data-remove=' + room + ' type="button">×</button></b>' +
    '<br/><div class="col-md-5">Start Date<br/><input type="date" class="sdate col-md-4 form-control" /></div>' +
    '<div class="col-md-5">End Date<br/><input type="date" class="edate form-control" /></div>' +
    '</div><div class="clearfix"></div><hr/>');
  });
}

$('body').on("click", ".delRoom", function(){
  console.log($(this).attr('data-remove'));
  removeCartCookie($(this).attr('data-remove'));
});

$('.bReserve').click(function(){
  var hotelID = $(this).attr('data-hotel');
  var roomID = $(this).attr('data-room');
  var cookie = getCartCookie();
  if(!cookie){
    setCartCookie(hotelID + ',' + roomID);
  }
  else{
    appendCartCookie(hotelID + ',' + roomID);
  }
});

</script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
<script src="/javascripts/rooms.js"></script>
</body>

</html>
